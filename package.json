{
  "name": "siripun-ins",
  "version": "0.2.1",
  "private": true,
  "description": "Source-based INS food additive lookup for WordPress + Cloudflare Workers",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "data:validate": "node scripts/validate-data.mjs",
    "data:check-sources": "node scripts/check-sources.mjs",
    "data:import-thai-fda": "node scripts/import-thai-fda.mjs",
    "data:import-thai-fda:apply": "node scripts/import-thai-fda.mjs --apply",
    "data:preview-thai-fda": "node scripts/preview-thai-fda-database.mjs",
    "dev": "npx wrangler dev",
    "deploy": "npx wrangler deploy",
    "data:sync": "node scripts/sync-public-data.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "engines": {
    "node": ">=22"
  }
}
